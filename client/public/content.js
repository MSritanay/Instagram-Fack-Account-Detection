
// This script will be injected into the Instagram page
// to scrape data and send it back to the popup.

chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
    // The main analysis logic is in instagram_analyzer.js
});