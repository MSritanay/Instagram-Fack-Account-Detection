## Packages
framer-motion | Smooth page transitions and UI animations
recharts | Visualizing risk scores and statistics
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
The app uses Replit Auth for authentication.
The API endpoints are defined in `@shared/routes`.
The app focuses on detecting scams in messages and profiles.
The design should be "clean professional security-focused".
